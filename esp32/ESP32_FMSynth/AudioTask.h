#pragma once // Preprocessor guard ensuring this header is included only once per compilation

// Public function declaration to initialize and spin up the real-time audio thread
void startAudioTask();